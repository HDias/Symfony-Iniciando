{% extends '::base.html.twig' %}

{% set title = 'Catalogo Edit' %}

{% block title -%}
    {{ parent() ~ title}} 
{% endblock %}

{% block body -%}
    <h1>{{ title }}</h1>


    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('catalogo') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
